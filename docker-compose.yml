
services:
  ecotoxicology-predict-fastapi:
    mem_limit: 512m
    memswap_limit: 512m
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8890:8888"
    volumes:
      - .:/app
      - ./service/model:/app/service/model
    restart: always
    networks:
      - ecotoxicology-predict-fastapi
    environment:
      - PYTHONUNBUFFERED=1      docker volume prune
    env_file:
      - .env

networks:
  ecotoxicology-predict-fastapi:
    driver: bridge